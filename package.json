{
  "name": "voluntariando",
  "version": "1.0.0",
  "description": "Plataforma de voluntariado comunitario — Cloudflare Worker + Firebase",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
